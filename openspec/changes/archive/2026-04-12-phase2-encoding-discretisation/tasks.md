# Tasks: Phase 2 Encoding and Discretisation

## Phase 1: Shared foundation and guardrails
- [x] 1.1 Add shared contract tests in `tests/test_base_transformer.py` and a new validation-focused test module for Phase 2 helper behavior covering selected-column resolution, categorical/numeric type checks, duplicate targets, learned trailing-underscore attrs, generated-column collision checks, and unsupported policy/bin configuration from `specs/encoding/spec.md` (Encoding Validation and Lifecycle) and `specs/discretisation/spec.md` (Discretisation Validation and Lifecycle).
- [x] 1.2 Extend `src/spark_feature_engine/_validation.py` with reusable Phase 2 helpers for categorical/numeric variable discovery, deterministic option normalization, generated-output collision detection, learned-state checks, and discretisation boundary validation until the new Phase 1 tests pass.
- [x] 1.3 Update `src/spark_feature_engine/base.py` only as needed to support fitted estimator/model patterns and shared learned-state utilities required by Phase 2, keeping existing Phase 1 behavior green.

## Phase 2: Encoding
- [x] 2.1 Write `tests/encoding/test_one_hot.py` for learned category expansion, deterministic generated-column naming, collision failures, unseen all-zero rows, non-selected-column stability, learned attrs, and Spark-native plan checks from `specs/encoding/spec.md` (One-Hot Encoding; Native Spark Encoding Execution).
- [x] 2.2 Implement `src/spark_feature_engine/encoding/one_hot.py` and `src/spark_feature_engine/encoding/__init__.py` so `OneHotEncoder.fit(df) -> OneHotEncoderModel` passes the new one-hot tests with native Spark operations only.
- [x] 2.3 Write `tests/encoding/test_ordinal.py` for deterministic mappings, selected-column replacement, unseen policies (`ignore` / `encode` / `raise`), learned attrs, non-selected-column stability, and Spark-native execution from `specs/encoding/spec.md` (Ordinal Encoding; Encoding Validation and Lifecycle; Native Spark Encoding Execution).
- [x] 2.4 Implement `src/spark_feature_engine/encoding/ordinal.py` so `OrdinalEncoder.fit(df) -> OrdinalEncoderModel` passes the new ordinal tests without UDFs.
- [x] 2.5 Write `tests/encoding/test_count_frequency.py` for count mode, frequency mode, unseen policies, learned attrs, deterministic mappings, non-selected-column stability, and Spark-native execution from `specs/encoding/spec.md` (Count and Frequency Encoding; Encoding Validation and Lifecycle; Native Spark Encoding Execution).
- [x] 2.6 Implement `src/spark_feature_engine/encoding/count_frequency.py` so `CountFrequencyEncoder.fit(df) -> CountFrequencyEncoderModel` passes the new count/frequency tests with native Spark joins/expressions.
- [x] 2.7 Write `tests/encoding/test_rare_label.py` for frequency-threshold grouping, minimum-category bypass, maximum frequent-category cap, learned attrs, non-selected-column stability, validation failures, and Spark-native execution from `specs/encoding/spec.md` (Rare Label Encoding; Encoding Validation and Lifecycle; Native Spark Encoding Execution).
- [x] 2.8 Implement `src/spark_feature_engine/encoding/rare_label.py` so `RareLabelEncoder.fit(df) -> RareLabelEncoderModel` passes the new rare-label tests without Python UDF nodes.

## Phase 3: Discretisation
- [x] 3.1 Write `tests/discretisation/test_equal_width.py` for learned equal-width boundaries, transformed bin assignments, future-extreme clipping into outer bins, numeric-vs-boundary-label outputs, learned attrs, validation failures, non-selected-column stability, and Spark-native plans from `specs/discretisation/spec.md` (Equal-Width Discretisation; Discretised Output Representation; Native Spark Discretisation Execution).
- [x] 3.2 Implement `src/spark_feature_engine/discretisation/equal_width.py` and `src/spark_feature_engine/discretisation/__init__.py` so `EqualWidthDiscretiser.fit(df) -> EqualWidthDiscretiserModel` passes the new equal-width tests with native Spark bucketing/expressions only.
- [x] 3.3 Write `tests/discretisation/test_equal_frequency.py` for approximate quantile-derived boundaries, ordered contiguous bins, future-extreme clipping, numeric-vs-boundary-label outputs, learned attrs, validation failures, non-selected-column stability, and Spark-native plans from `specs/discretisation/spec.md` (Equal-Frequency Discretisation; Discretised Output Representation; Native Spark Discretisation Execution).
- [x] 3.4 Implement `src/spark_feature_engine/discretisation/equal_frequency.py` so `EqualFrequencyDiscretiser.fit(df) -> EqualFrequencyDiscretiserModel` passes the new equal-frequency tests without UDFs.
- [x] 3.5 Write `tests/discretisation/test_arbitrary.py` for user-supplied boundary assignment, left-open/right-closed interval semantics, out-of-range policies (`ignore` / `raise`), numeric-vs-boundary-label outputs, stateless behavior, validation failures, non-selected-column stability, and Spark-native plans from `specs/discretisation/spec.md` (Arbitrary Discretisation; Discretised Output Representation; Discretisation Validation and Lifecycle; Native Spark Discretisation Execution).
- [x] 3.6 Implement `src/spark_feature_engine/discretisation/arbitrary.py` so `ArbitraryDiscretiser` passes the new arbitrary-discretisation tests with parameter-driven native Spark expressions only.

## Phase 4: Package integration and verification
- [x] 4.1 Update `src/spark_feature_engine/__init__.py` to export all Phase 2 encoding and discretisation classes, and add any package-level import coverage needed in `tests/test_package_setup.py`.
- [x] 4.2 Run targeted pytest modules after each feature area and then execute full verification with `pytest`, `mypy src tests`, and `python -m build`, confirming all Phase 1 and Phase 2 suites remain green.
- [x] 4.3 Manually review representative Spark execution plans from the new encoding and discretisation tests to confirm no `PythonUDF`, `BatchEvalPython`, or `ArrowEvalPython` nodes appear in fit/transform plans.
